# MyAgent 架构改造方案（REFACTOR_PLAN）

> 编制日期：2026-08-04
> 编制依据：`BRIEFING.md`（赛事规则/评分表/角色映射）、`DIAGNOSIS.md`（P0 诊断）、`dev-team-platform/FRAMEWORK.md`（四层记忆/经验效用评分/知识保质期/提示词长度纪律）、`dev-team-platform/PLAN.md`
> 文档性质：**纯方案文档。编制过程中未修改任何源码、配置或数据文件。** 三项改造均需用户逐项确认后才可执行。

---

## 0. 勘察基线（本次实测数据，非估算）

| 项目 | 实测值 | 采集方式 |
|------|--------|---------|
| 角色 prompt 份数 | 18 份 | `backend/core/agent/roles/*/prompt.txt` |
| prompt 总字数 | 75,998 字 | `wc -m` 逐份统计 |
| 角色池注册数 | 17 个 | `data/role_pool.json` |
| 运行时加载数 | 17 个 | 后端启动日志 `[RoleLoader] 已加载 17 个角色` |
| 预设工作组 | 10 条 | `data/workgroups/*.json` |
| 记忆模块代码量 | 2,930 行 | `backend/core/memory/*.py` |
| 经验管理器 | `ExperienceManager`（orchestrator.py L179-379） | 已有记录/匹配/注入，**无效用评分** |
| 知识库 | `KnowledgeBase`（knowledge_base.py 424 行） | 三元组结构，**无 TTL** |

### 0.1 一个必须先说的发现：`secretary` 是孤儿 prompt

- `backend/core/agent/roles/secretary/prompt.txt` 存在，**5,371 字**
- `data/role_pool.json` 的 17 个角色里**没有 secretary**
- 后端启动日志的角色注册列表里**也没有 secretary**

结论：这 5,371 字**从未被加载过**，纯占磁盘。秘书能力实际由 `orchestrator.py` 里的 `Secretary` 类以硬编码方式实现，不走 prompt 文件。

**处置建议**：这份文件不参与方案 A 的精简（精简一个不加载的文件没有收益），建议在方案 B 执行时一并作为"死文件"处理。方案 A 的有效工作量因此从 18 份降为 17 份、75,998 字降为 **70,627 字**。

---

# 方案 A：角色 Prompt 精简

## A1. 问题诊断

FRAMEWORK.md 的提示词长度纪律：

| 角色类型 | 黄金区间 | 说明 |
|---------|---------|------|
| 简单工具人 | 300-500 字 | 单一动作，无决策 |
| 标准执行者 | 600-900 字 | 有判断，但路径固定 |
| 复杂决策者 | 900-1200 字 | 需权衡、分阶段 |

超 2000 词元后幻觉率 +34%。当前 17 份加载中的 prompt，**没有一份在区间内**，最小的 translator 2,412 字也是上限的 2 倍，coach 7,201 字是上限的 6 倍。

### 真正的代价：prefill 词元，不是磁盘

角色 prompt 每次调用都作为 system message 完整送进上下文。以本次实测跑通的 `dev_full` 流水线（9 步）为例：

| 步骤 | 角色 | 当前 prompt 字数 |
|------|------|-----------------|
| 1,2,4 | coach ×3 | 7,201 × 3 = 21,603 |
| 3 | designer | 5,303 |
| 5 | developer | 4,321 |
| 6 | inspector | 4,084 |
| 7 | tester | 4,815 |
| 8 | deployer | 4,108 |
| 9 | cleaner | 4,457 |
| **合计** | | **48,691 字** |

按 Qwen2.5 中文 tokenizer 约 0.7 token/字估算，**单次 dev_full 请求的 system prompt prefill ≈ 34,000 词元**。这是纯开销——用户消息本身通常不足 100 字。

**对应评分表**：速度分 20 分。prefill 是首字延迟（TTFT）的主要构成，也是演示视频里最直观的"卡不卡"。

## A2. 精简目标与分档

| 档位 | 目标字数 | 角色 |
|------|---------|------|
| 复杂决策者 | 900-1200 | master、coach、handoff_receiver、designer |
| 标准执行者 | 600-900 | developer、inspector、tester、deployer、knowledge_retriever、hr_manager |
| 简单工具人 | 300-500 | quality_checker、cleaner\* |

\* cleaner 若按方案 B 裁撤则不需精简。

## A3. 逐份精简策略表

| # | 角色 | 现状 | 目标 | 降幅 | 主要精简手法 |
|---|------|------|------|------|-------------|
| 1 | coach | 7,201 | 1,150 | 84% | 删「8.工具」整章、「7.记忆」整章、4 个输出模板、教学清单；保留注意力管理表 + 三步走 + 派单必填字段 |
| 2 | designer | 5,303 | 1,050 | 80% | 删工具章/记忆章，设计规范外置为附件引用 |
| 3 | tester | 4,815 | 800 | 83% | 删工具章/记忆章，测试用例模板压成 3 行 |
| 4 | developer | 4,321 | 800 | 81% | 删工具章/记忆章/技术债表，7 项自查压成 7 行 |
| 5 | master | 4,209 | 1,100 | 74% | 保留调度逻辑与防火墙，删记忆章与输出模板 |
| 6 | knowledge_retriever | 4,157 | 800 | 81% | 删工具章/记忆章 |
| 7 | deployer | 4,108 | 750 | 82% | 删工具章/记忆章，部署清单压成表 |
| 8 | inspector | 4,084 | 800 | 80% | 删工具章/记忆章，检查项压成清单 |
| 9 | handoff_receiver | 3,879 | 950 | 76% | 保留诊断流程，删工具章/记忆章 |
| 10 | hr_manager | 3,547 | 700 | 80% | 删工具章/记忆章 |
| 11 | quality_checker | 2,821 | 600 | 79% | 删工具章/记忆章，问题分级压成表 |
| 12-17 | creative / translator / visual_analyzer / writer / scheduler / cleaner | 22,082 | 0 | 100% | **由方案 B 整体裁撤，不投入精简工时** |
| — | secretary（孤儿） | 5,371 | 0 | 100% | 不加载，作死文件处置 |
| | **加载中合计** | **70,627** | **9,500** | **86.5%** | |

## A4. 五条通用精简手法（可复用到任何一份）

1. **删「8. 工具」整章** —— 每份 300-600 字。这一章描述的能力（文件读写、搜索、Git）由代码层的工具注册表提供，模型无法通过读 prompt 获得任何新能力，纯属自我描述。**17 份合计约 7,000 字，是最大的单笔冗余。**
2. **删「7. 记忆」整章** —— 每份 250-450 字。记忆的读写由 `WorkingMemory` / `KnowledgeBase` 代码控制，不是模型自主行为。（注：多数文件此处标题编号错乱，`## 7. 记忆` 下面是 `### 6.1/6.2/6.3`，属存量笔误。）
3. **输出模板降级为骨架** —— 完整 Markdown 模板（PROJECT_PLAN、STATUS、报告格式）每份 500-1500 字。改为 3-5 行字段清单，模型照样能生成正确结构。
4. **表格化替代散文** —— 「不越界」6 条 + 「纪律」5 条 + 「自查」9 条，用一行一条的清单，去掉每条后面的解释性从句。
5. **保留粉红大象结构** —— 负向约束必须带正面替代（"不写代码 → 派给开发员"）。这条**不能为了省字砍掉**，砍掉会导致模型反而去做被禁的事。

## A5. 完整精简样例（3 份，可直接落盘）

### A5-1. coach（7,201 → 1,142 字）

```markdown
# 教练 (Coach) · 系统提示词

## 1. 身份
你是「教练」，开发团队的产品教练。你**不写代码、不出样图、不做巡检测试部署**——这些派给对应角色。你的工作是：把用户模糊的想法变成清晰方案，把方案拆成团队能执行的任务包。

## 2. 注意力管理（按阶段激活，不在列表的这轮别做）
| 阶段 | 激活 | 不激活 |
|------|------|--------|
| Phase 0 需求发现 | 需求探索 + 竞品调研 + 教学 + 认知适配 | 派单、防火墙 |
| Phase 1 界面设计 | 派单(设计师) + 答疑 | 调研、防火墙 |
| Phase 2 编码 | 派单(开发/巡检/测试) + 防火墙 | 调研、教学(用户问才教) |
| Phase 3 部署 | 派单(部署) + 防火墙 | 调研、教学 |

## 3. Phase 0 需求发现三步走
1. **开放式理解**（2-3 轮）：复述你的理解 → 问一个你真正好奇的关键问题 → 追问到能在脑子里跑通主流程。不用选项菜单，不预设"第N轮"节奏。
2. **深度调研**（你自己做，别让用户等）：同类产品做对/做错了什么、领域最佳实践、用户技术栈下哪些方案是坑、有没有被忽略的视角。回来只给 **3-5 条关键发现 + 取舍建议**，不扔原始列表。
3. **自查后产出 PROJECT_PLAN**：跟竞品的真正区别是什么？有没有更深的用户价值？会留技术债吗？别人听完会说"就这？"还是"有意思"？——四问满意才产出。

## 4. 先做后解释
用户说"给我看效果" → 直接让设计师出草图或写 demo，产出后再解释。不要用文字描述"我能做到什么"。手头没有可用工具时如实说，不假装有。

## 5. 输出克制
- 认知适配的判断：不吐，直接调整语气深度
- 调研结果：只吐关键发现 + 取舍建议
- 自查四问：不吐，自己过一遍
- 产出计划：吐计划本身，不吐推导过程

## 6. 派单铁律
**派设计师时必须传技术栈**，不传是严重失职（会导致设计师出纯 HTML，开发员必须重写）。任务包必填字段：
- 技术栈：前端框架 / UI 库 / 状态管理
- 路由方案、构建工具、部署方式
- 环境变量需求（有则列出，无则写"无"）
- 设计产出格式（必须与开发技术栈一致）
- 输入材料、执行标准、输出要求

Phase 2 每模块循环：开发写 → 巡检查 → 不通过打回 → 通过则测试验 → 下一模块。

## 7. 项目状态维护
每次状态变化**立即**更新 `data/projects/{project_name}/PROJECT_STATUS.md`，不等会话结束。
- 粒度到模块/页面级，三种状态：待完成 / 进行中 / 已完成
- 进行中同时只允许 1-2 个
- 技术债只列"紧急/高"两级
- 标准：新成员读完能立刻知道"下一个做什么、谁在做、哪卡住了"

## 8. 教学纪律
- 第一次教概念：术语 + 大白话；第二次：术语 + 简短提示；第三次起：直接用术语
- 解释只说两句：①这是什么 ②为什么重要
- 只在用户需要时教，不主动塞知识。用户说"别解释直接做"就停
- 用户能精准描述技术方案时，进入对等讨论，不要再用"我来教你"的语气

## 9. 回复格式
每次回复开头标注：`[阶段: {phase}] {简短说明}`

## 10. 交付前自查
需求覆盖明确点+隐含需求 / 竞品≥3个 / 子任务≤30分钟 / 教学适配认知水平 / STATUS 已更新 / **派设计师含技术栈字段**
```

### A5-2. developer（4,321 → 812 字）

```markdown
# 开发 (Developer) · 系统提示词

## 1. 身份
你是「开发员」，团队的代码工匠。你按设计稿和标准把需求变成代码。你**不做架构决策**（主控和巡检的事）、**不写测试用例**（测试员的事）、**不做部署**、**不自选技术栈**（已在项目规划中确定）。

核心纪律：**每次开工前重读对应标准文件，不靠记忆。**

## 2. 职责
- 按模块规格和设计稿实现功能
- 收到巡检打回意见后返工，标注修改位置和原因，不引入新问题
- 收到设计图做 30 秒可行性扫描：只答"能做"或"第X处做不了因为Y"，不写代码不讨论
- 主动识别技术债并记录（位置/类型/级别/预估成本），每次迭代拿 20% 时间偿还

## 3. 硬性约束
| 项 | 上限 |
|---|------|
| 单文件 | 300 行 |
| 单函数/组件 | 50 行 |
| 单次改动 | 500 行（超过拆多次） |
| 嵌套深度 | 3 层 |
| 参数个数 | 5 个（超过用对象） |
| 圈复杂度 | 10 |

## 4. 代码规范
- 命名：变量 camelCase、组件 PascalCase、常量 UPPER_SNAKE
- 类型：TypeScript 优先，**不用 any**
- 样式：CSS Modules / Tailwind，不用内联样式
- 注释：只解释"为什么"，不解释"做了什么"
- 依赖：只引必要的，优先复用已有；新增需说明理由和替代方案

## 5. 代码纪律
- 不写"临时方案以后再改"——第一次就写对。迫于进度必须写时，同步记入技术债清单并标注偿还时间
- 不注释掉代码块保留（用 Git 管版本）
- 不留未使用的变量和导入

## 6. 技术债分级
紧急（阻碍开发/致 bug/安全）→ 当前迭代修 | 高（违反核心标准）→ 下迭代 | 中（代码异味）→ 近期 | 低（风格）→ 有空再说

## 7. 提交前 7 项自查（不全绿不交巡检）
1. **错误处理**：异步有 try/catch、Promise 有 .catch()、可空值有判空
2. **无硬编码**：颜色/URL/配置/魔法数字 → 常量或配置
3. **清洁**：删 console.log、debugger、注释掉的旧代码、未用 import
4. **命名语义化**：不用 a/b/t1；同义词收敛（别 updateUser 和 modifyUser 混用）
5. **边缘情况**：空数组/空串/0/null/undefined 不崩；有加载态、错误态、空态
6. **类型完整**：无 any，Props 有接口，参数和返回值有类型
7. **测试可跑**：新逻辑有覆盖，现有测试全过

## 8. 输出格式
```
## 模块实现：{模块名}
### 文件清单
{路径 — 用途}
### 代码
{按文件给完整代码，标注语言和路径}
### 实现说明
状态管理 / 错误处理策略 / 边界情况覆盖
```
返工时改为：`## 返工修复：{模块名}` + 修复清单（原意见 → 做了什么 → 文件:行号）+ 修改后完整代码。
```

### A5-3. quality_checker（2,821 → 604 字）

```markdown
# 质检 (Quality Checker) · 系统提示词

## 1. 身份
你是「质检员」，团队的守门人。你不关心内容是谁写的，只关心对不对、能不能用。你是审计者不是创作者，产出是**检查报告**。

与巡检员的分工：巡检员查**代码架构**，你查**内容质量**（事实准确性、逻辑连贯性、完整性、一致性）。

## 2. 四项检查
- **事实核查**：技术声明要有官方文档或权威来源；数据引用要可追溯；URL 要可访问且相关。无法验证的标"未经验证"，**不判定为错误**。
- **逻辑审查**：前提→推理→结论是否完整；有无以偏概全、因果倒置、虚假二分、循环论证；前后有无矛盾。
- **完整性**：前置条件、步骤是否跳步、异常流程、边界情况、术语定义、参考资料。
- **一致性**：同一概念是否全文同一术语；同一数据在不同位置是否一致；格式风格是否统一。

## 3. 边界
- 你**不修改**原始内容，只报告问题，修复由原作者执行
- 你不做技术可行性判断（巡检/开发的事），不做创意评价，不查代码语法
- 你不知道作者是谁（下发时已去标识化），审查标准对所有人一致
- "我觉得这样更好"**不是**有效意见；"此处可能歧义，建议改为 X"才是
- 每个问题必须三件套：问题描述 + 依据 + 修复建议

## 4. 问题分级
| 级别 | 定义 | 结论 |
|------|------|------|
| 严重 | 事实错误、逻辑矛盾、关键信息缺失 | 不通过 |
| 一般 | 逻辑跳跃、表述歧义、非关键缺失 | 通过但有建议 |
| 提示 | 术语/风格/格式 | 通过 |

## 5. 输出格式
```
## 质检报告
审查对象：{标识}
结论：{通过 / 不通过 / 通过但有建议}    评分：{1-5}    置信度：{高/中/低}

### 严重问题（必须修复）
1. [类型] {位置}：{描述} | 依据：{} | 建议：{}
### 一般问题（建议修复）
### 提示项（可选优化）

### 四维评估
事实准确性 / 逻辑连贯性 / 信息完整性 / 一致性：{通过|有问题} {详情}
```
短内容（邮件、简短回复）用简化格式：`结论 + 问题一句话`。

## 6. 交付前自查
逐条验证而非抽查 / 不确定的标"待验证"不武断判错 / 覆盖三类逻辑谬误 / 建议具体可操作（不写"需要改进"）/ 给出 1-5 评分
```

## A6. 精简前后对照与 prefill 降幅预估

### 单份对照

| 角色 | 前 | 后 | 降幅 | 档位达标 |
|------|-----|-----|------|---------|
| coach | 7,201 | 1,142 | −84.1% | ✓ 复杂决策者 900-1200 |
| developer | 4,321 | 812 | −81.2% | ✓ 标准执行者 600-900 |
| quality_checker | 2,821 | 604 | −78.6% | ✓ 标准执行者下限 |

### 流水线级 prefill 降幅（dev_full，9 步，实测跑通的链路）

| 项 | 精简前 | 精简后（预估） | 降幅 |
|----|--------|--------------|------|
| system prompt 总字数 | 48,691 | 8,050 | −83.5% |
| ≈ 词元数（0.7 tok/字） | ≈ 34,084 | ≈ 5,635 | −83.5% |
| 假设 prefill 2,000 tok/s | ≈ 17.0 s | ≈ 2.8 s | **省 ≈ 14 s** |
| 假设 prefill 1,000 tok/s | ≈ 34.1 s | ≈ 5.6 s | **省 ≈ 28 s** |

> 上表 prefill 速率为占位假设，**必须以 A7 的实测替换后再写进任何提交材料**。禁止把预估值当实测值报给评委。

## A7. A/B 测速方案

**原则：同一台机器、同一模型、同一 prompt 输入、只换角色 prompt 版本。**

1. **准备**：保留 `roles/{role}/prompt.txt` 为 A 组（现状），新增 `roles/{role}/prompt.slim.txt` 为 B 组。`loader.py` 已有 prompt 文件优先加载逻辑，只需加一个环境变量开关 `PROMPT_VARIANT=slim` 决定读哪个文件——**不删原文件，随时可切回**。
2. **指标**：① TTFT（首字延迟）② 整条 dev_full 流水线端到端耗时 ③ 输出质量人工打分（1-5）④ 实际 prompt_tokens（从 llama-server 的 usage 字段直接读，不用估算）
3. **样本**：3 类任务 × 5 次重复 = 15 组/版本。取中位数，丢弃首次（避开模型冷启动）。
4. **质量对照**：同一批输出交由 quality_checker 盲评（不告知哪版是精简版），防止"快了但变傻了"。
5. **判据**：B 组 prompt_tokens 降幅 ≥ 70% **且** 质量分下降 ≤ 0.3 分 → 采纳。质量分掉超过 0.5 → 回退该份重新精简。

## A8. 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| 删掉的约束实际在起作用，精简后模型行为漂移 | **中** | 逐份精简、逐份 A/B，不批量替换；保留原文件可秒回退 |
| 删负向约束导致模型做被禁的事 | 中 | A4 手法 5：负向约束一律保留且带正面替代 |
| 输出模板简化后下游解析失败 | 低 | 先检查是否有代码正则解析角色输出；本次勘察未发现强解析，但执行前需复查 |
| 17 份逐份精简耗时超预算 | 低 | 方案 B 先执行可直接砍掉 6 份，实际只需精简 11 份 |

## A9. 方案 A 结论卡

| 项 | 内容 |
|----|------|
| 预估工时 | **8 小时**（11 份 × ~30 分钟精简 + 2 小时 A/B 测速 + 1 小时返修） |
| 风险等级 | **中**（可逐份回退，但需实测护航） |
| 最小可交付版本 | **只精简 dev_full 链路上的 7 份**（coach/designer/developer/inspector/tester/deployer + master），约 3.5 小时，已覆盖演示视频的主链路，prefill 降幅仍达 ~83% |
| 对应评分 | 速度分 **20 分** 的主要抓手；同时降低幻觉率，间接护住功能完整性分 |
| 前置依赖 | 建议在方案 B 之后执行（先裁角色，避免精简了又删掉） |

---

# 方案 B：裁撤 6 个非编制角色

## B1. 待裁清单

| 角色 | prompt 字数 | 在 DevTeam 编制内 | 当前唯一价值 |
|------|-----------|-----------------|-------------|
| visual_analyzer | 4,970 | ✗ | 视觉分析（需 VL 模型，赛事演示不涉及） |
| cleaner | 4,457 | ✗ | 文件清理（可由 developer 兼任） |
| creative | 3,961 | ✗ | 头脑风暴（与 coach 职能重叠） |
| scheduler | 3,832 | ✗ | 日程（与开发主线无关） |
| writer | 2,565 | ✗ | 写作（与 coach/knowledge_retriever 重叠） |
| translator | 2,412 | ✗ | 翻译 |
| **合计** | **22,197** | | |

加上孤儿 `secretary`（5,371 字），可清理 **27,568 字**。

## B2. 物理删除 vs 移出主流水线 —— 建议：**分层处置，不做一刀切**

| 处置层级 | 对象 | 做法 | 理由 |
|---------|------|------|------|
| **L1 物理删除** | `secretary/prompt.txt` | 直接删目录 | 从未被加载，删掉零风险，纯净化 |
| **L2 移出编制** | translator、scheduler、creative、visual_analyzer、writer | prompt 文件移到 `backend/core/agent/roles/_archived/`；从 `role_pool.json`、`agent config.yaml` 的 role_pool、工作组配置中移除 | **可逆**。评委若问"为什么只有这些角色"，回答"按 DevTeam 编制裁剪"比"我删了"更有说服力；万一演示需要还能 5 分钟接回来 |
| **L3 降级留用** | cleaner | 保留角色定义，但**从 4 条 dev 工作组的 pipeline 末位移除**，同时移除 `master.py` 的自动追加钩子；改为用户显式要求时才调用 | cleaner 在 4 条主力流水线里都是末位步骤，删掉直接缩短 4 条链路；但它的"构建校验"能力在部署演示里有用，不宜彻底删 |

**核心理由**：赛事评分看的是"角色编制是否合理、是否有清晰的团队设计"，不是"角色越少越好"。L2 的归档做法既拿到了精简收益，又保留了"我们做过取舍"的可解释性。而**物理删除的不可逆风险，在离赛事截止不到 59 小时的窗口里不值得冒**。

## B3. 受影响工作组逐条确认（10 条全覆盖）

| # | 工作组 | 现有 members | 现有 pipeline | 命中待裁角色 | 处置 |
|---|--------|-------------|--------------|-------------|------|
| 1 | dev_code_review | inspector, developer, tester, **cleaner** | inspector→inspector→developer→tester→**cleaner** | cleaner | 移除末位 cleaner，pipeline 5→4 步 |
| 2 | dev_design_only | coach, designer, quality_checker | coach→designer×3→quality_checker | **无** | ✓ 不受影响 |
| 3 | dev_full | coach, designer, developer, inspector, tester, deployer, **cleaner** | 9 步，末位 **cleaner** | cleaner | 移除末位，pipeline 9→8 步 |
| 4 | dev_modification | handoff_receiver, inspector, designer, developer, tester, deployer, **cleaner** | 12 步，末位 **cleaner** | cleaner | 移除末位，pipeline 12→11 步 |
| 5 | dev_tech_debt | inspector, developer, tester, **cleaner** | inspector→inspector→developer→tester→**cleaner**→inspector | cleaner | **注意：cleaner 不在末位**，后面还有 inspector。移除后为 5 步，需确认最后的 inspector 复检逻辑不依赖 cleaner 产出 |
| 6 | report_writing | knowledge_retriever, **writer**, quality_checker | knowledge_retriever→**writer**→quality_checker→**writer** | writer ×2 | ⚠ **主力被裁，流水线残废**。二选一：(a) 整组删除；(b) writer 的两步改派 **coach**（coach 有产出结构化文档的能力）。**建议 (a) 整组删除**——report_writing 不在赛事演示链路上 |
| 7 | research_investigation | knowledge_retriever, **creative**, quality_checker | knowledge_retriever→**creative**→quality_checker→knowledge_retriever | creative | creative 那一步改派 **coach**（coach 本就负责竞品调研与取舍分析），pipeline 步数不变 |
| 8 | schedule_planning | **scheduler**, **creative**, quality_checker | **scheduler**→**creative**→quality_checker→**scheduler** | scheduler ×2 + creative | ⚠ **全灭**，剩 quality_checker 一个。**整组删除** |
| 9 | translation_task | **translator**, quality_checker | **translator**→quality_checker→**translator** | translator ×2 | ⚠ **主力全灭**。**整组删除** |
| 10 | visual_analysis_task | **visual_analyzer**, **writer** | **visual_analyzer**→**writer** | 两个全中 | ⚠ **全灭**。**整组删除** |

### 结论：10 条 → 6 条

- **删 4 条**：report_writing、schedule_planning、translation_task、visual_analysis_task
- **改 5 条**：dev_code_review、dev_full、dev_modification、dev_tech_debt（各移除 cleaner）、research_investigation（creative → coach）
- **不动 1 条**：dev_design_only

## B4. 代码硬引用清单（9 处，逐处给处置）

| # | 位置 | 内容 | 风险 | 处置 |
|---|------|------|------|------|
| 1 | `core/agent/lifecycle.py:19` | `DEFAULT_ROLE_POOL = ["master","knowledge_retriever","writer","quality_checker"]` | **高** —— 新建 Agent 的默认角色池含 writer | 必改：移除 writer，建议改为 `["master","coach","knowledge_retriever","quality_checker"]` |
| 2 | `core/agent/agent_schemas.py:81` | 同上的 `default_factory` | **高** | 必改，与 #1 保持一致 |
| 3 | `core/agent/agent_schemas.py:180-257` | 6 个待裁角色的内置定义条目 | 中 | 移除对应条目 |
| 4 | `data/agents/general_assistant/config.yaml` | `role_pool` 15 个，含全部 6 个待裁角色 | **高** —— 这是演示用的唯一 Agent | 必改：15 → 9（master, coach, knowledge_retriever, quality_checker, designer, developer, inspector, tester, deployer） |
| 5 | `core/role/master.py:661-673` | `_apply_cleanup_hook` 硬编码追加 cleaner | 中 | 移除整个钩子函数调用（函数体可留） |
| 6 | `core/agent/agent_generator.py:238-243` | 中文→角色 ID 映射含 写作/翻译/日程 | 低 | 移除三条映射 |
| 7 | `core/agent/agent_generator.py:101` | prompt 示例文本含 `"translator"` | 低 | 改示例为存留角色 |
| 8 | `core/memory/blackboard.py:404-414` | 角色 ID→中文名映射含 5 个待裁角色 | **无** | 纯显示用，留着不影响；建议清理但非必须 |
| 9 | `core/memory/exporter.py:17,80` | 默认导出 `roles=["writer","coach"]` | 低 | 改为 `["coach"]` |
| 10 | `core/role/loader.py:148` | writer 的内置 fallback prompt 字符串 | 低 | 移除该分支 |

### 容错性已验证

`loader.py` 的 `get_role()` 返回 `Optional`，未知角色 ID **不会抛异常**；`master.py` 的 cleanup 钩子仅在 `members` 含 cleaner 时才追加；关键词匹配仅作回退路径。因此**即使漏改某一处引用，系统不会崩溃，只会静默少一个角色**——这降低了本方案的执行风险，但也意味着**必须靠启动日志的角色计数来验收**，不能靠"没报错"来判断改对了。

## B5. 执行顺序（严格按此序，每步可独立验证）

1. 备份 `data/role_pool.json`、`data/workgroups/`、`data/agents/general_assistant/config.yaml`
2. prompt 文件移入 `_archived/`（L2）+ 删 secretary（L1）
3. 改 `role_pool.json`：17 → 11 个角色（同步清理 `gpu_allocation` 三个 GPU 组里的角色名）
4. 改 `general_assistant/config.yaml`：role_pool 15 → 9
5. 删 4 条工作组 JSON、改 5 条工作组 JSON
6. 改 B4 的 10 处代码引用
7. **验收**：重启后端，检查日志 `[RoleLoader] 已加载 N 个角色` 应为 **11**、`[Master] 已加载 M 个预设工作组` 应为 **6**；再跑一次 dev_full，确认 pipeline 为 **8 步且全绿**

## B6. 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| 漏改引用导致静默降级（不报错但角色缺失） | **中** | 靠启动日志计数验收（11/6），不靠"没报错" |
| dev_tech_debt 的末位 inspector 依赖 cleaner 产出 | 低 | 执行前读一遍该流水线的 step 定义确认 |
| 演示时评委问到被裁角色 | 低 | L2 归档而非删除，可现场解释"按编制裁剪，归档保留" |
| gpu_allocation 里残留已删角色名 | 低 | 步骤 3 明确要求同步清理 |

## B7. 方案 B 结论卡

| 项 | 内容 |
|----|------|
| 预估工时 | **2.5 小时**（配置改动 1h + 代码引用 0.5h + 验证 1h） |
| 风险等级 | **低-中**（全部可逆，有明确的日志验收口径） |
| 最小可交付版本 | **只做 L3（4 条 dev 工作组移除 cleaner）+ 改 general_assistant 的 role_pool**，约 40 分钟，即可让主演示链路从 9 步缩到 8 步、少加载 4,457 字 |
| 对应评分 | 功能完整性中的"架构合理性"；间接贡献速度分（少 1 步 = 少一整轮推理）；**并为方案 A 减少 6 份精简工作量** |
| 建议次序 | **三个方案里第一个执行**——它是方案 A 的减负前提 |

---

# 方案 C：记忆系统补齐

## C1. 现状与缺口

BRIEFING 第十章的判断（"经验效用评分 + 知识保质期全库为零"）经本次勘察**确认属实**：

| 能力 | 现状 | 位置 |
|------|------|------|
| 经验记录/匹配/注入 | ✓ 已有 | `orchestrator.py` `ExperienceManager` L179-379 |
| 经验**效用评分** | ✗ **零** | `ExperienceRecord` 只有 `success_count`（只增不减） |
| 经验**淘汰机制** | ✗ **零** | 无上限、无删除、无负反馈 |
| 知识三元组存储/检索 | ✓ 已有 | `knowledge_base.py` 424 行 |
| 知识**保质期 TTL** | ✗ **零** | 三元组只有 `created_at`/`updated_at`，无过期概念 |
| 知识置信度衰减 | ✗ 零 | `confidence` 只增（`add_triple` 里 `+0.1*confidence`），永不衰减 |

### 为什么这是最值钱的一项

Harvard 2025 的结论：**把所有经验都存下来（add-all），效果比什么都不存还差**——因为过时和错误的经验会污染上下文，模型会照着错的做。当前 `ExperienceManager` 正是标准的 add-all：只加不减、只增不降、无上限。

同理，知识三元组里"项目用 React 18"这类事实，6 个月后可能已经不成立，但当前系统会**永远以同样的置信度注入**，且因为 `occurrences` 累加，越老的知识反而越"可信"——这是一个**会随时间恶化的设计缺陷**。

## C2. 子方案 C-1：经验效用评分

### C2-1. 数据结构（在现有 dataclass 上**加字段**，不改已有字段）

```python
@dataclass
class ExperienceRecord:
    # ===== 以下 8 个字段完全不动 =====
    task_type: str
    keywords: List[str]
    context: str
    constraints: List[str]
    successful_approach: List[str]
    failed_attempts: List[str]
    timestamp: str = ""
    success_count: int = 1

    # ===== 新增 5 个字段，全部带默认值 =====
    utility_score: float = 0.0      # 效用分，可正可负
    applied_count: int = 0          # 被注入并实际采用的次数
    last_applied: str = ""          # 最近一次被采用时间 (ISO)
    evaluator_notes: str = ""       # 经验评估员的判语
    status: str = "active"          # active | probation | archived
```

**为什么这是加法**：`from_dict()` 已经写成 `{k: v for k, v in data.items() if k in cls.__dataclass_fields__}`——**老 JSON 文件缺新字段会自动用默认值填充，不会抛异常**。已有的 `data/experiences/git_push.json` 无需迁移即可继续加载。这一点已通过阅读源码确认，不是推测。

### C2-2. 评分规则（对齐 FRAMEWORK.md）

| 事件 | 分值 | 触发时机 |
|------|------|---------|
| 经验被注入后任务成功 | **+1** | 流水线全绿收尾 |
| 经验被注入但任务仍失败 | **−1** | 任一步骤失败 |
| 经验被注入且**误导**了执行（照做反而错） | **−2** | 经验评估员判定 |
| 经验未被匹配到 | 0 | 不惩罚 |

### C2-3. 淘汰机制

| 规则 | 阈值 | 动作 |
|------|------|------|
| 池容量上限 | 15 条/task_type | 超出时淘汰 `utility_score` 最低的一条 |
| 立即删除线 | `utility_score ≤ −3` | 直接移出 active 池，标 `archived` |
| 察看期 | `utility_score < 0` | 标 `probation`，**降权注入**（排在正分经验之后，且不占满 3 条注入名额） |
| 注入排序 | — | 由现有的 `success_count` 降序，改为 **`utility_score` 降序 → `success_count` 降序** |

### C2-4. 落地位置（精确到方法）

| 改动点 | 文件:行 | 性质 |
|--------|--------|------|
| `ExperienceRecord` 加 5 字段 | `orchestrator.py:149-166` | **加字段** |
| 新增 `vote(record_id, delta, note)` 方法 | `ExperienceManager` 内新增 | **加方法** |
| 新增 `_evict_if_full(task_type)` 方法 | `ExperienceManager` 内新增 | **加方法** |
| `find()` 排序键改为 utility 优先 | `orchestrator.py:319` | **改 1 行**（唯一一处修改，非新增） |
| `get_injection()` 过滤 `status=="archived"` | `orchestrator.py:331` 附近 | **加 1 个过滤条件** |
| 新增 `get_utility_report()` 供演示展示 | `ExperienceManager` 内新增 | **加方法** |

**总计：改动 2 行，新增 4 个方法 + 5 个字段。零删除，零签名变更。**

## C3. 子方案 C-2：知识保质期（TTL）

### C3-1. 数据结构（三元组加 3 个键）

```python
triple = {
    # ===== 以下 9 个键完全不动 =====
    "id", "subject", "relation", "object", "source_role",
    "confidence", "created_at", "updated_at", "occurrences",

    # ===== 新增 3 个键 =====
    "knowledge_type": "technical",   # technical | security | platform | permanent
    "expires_at": "2027-02-04T...",  # ISO，由 created_at + TTL 计算
    "staleness": 0.0,                # 0.0=新鲜，1.0=完全过期
}
```

### C3-2. TTL 表（对齐 FRAMEWORK.md）

| 知识类型 | TTL | 举例 | 过期动作 |
|---------|-----|------|---------|
| `technical` | **6 个月** | "项目用 React 18"、"构建工具是 Vite" | 置信度打 0.5 折，标记待复核 |
| `security` | **3 个月** | "该依赖有 CVE-2025-xxxx" | 置信度打 0.3 折，**强制复核** |
| `platform` | **2 个月** | "ROCm 6.2 在该实例上可用" | 置信度打 0.3 折 |
| `permanent` | **不过期** | "用户偏好中文回复"、"用户是非技术背景" | — |

### C3-3. 落地位置

| 改动点 | 文件:行 | 性质 |
|--------|--------|------|
| `add_triple()` 加 `knowledge_type` 参数（默认 `"technical"`）并计算 `expires_at` | `knowledge_base.py:92-147` | **加参数**（带默认值，所有现有调用方无需改） |
| 新增 `_compute_staleness(triple)` | 新增私有方法 | 加方法 |
| `search()` 加权时乘以新鲜度系数 | `knowledge_base.py:208` | **改 1 行**（`weighted_score` 表达式追加 `* freshness`） |
| `assembly_context()` 对过期三元组加"⚠ 可能已过期"前缀 | `knowledge_base.py:320` | **改 1 行** |
| 新增 `sweep_expired()` 清扫方法（对齐已有的 `remove_low_confidence()` 风格） | 新增公开方法 | 加方法 |
| 新增 `get_freshness_report()` 供演示 | 新增公开方法 | 加方法 |

**兼容性**：老三元组没有 `knowledge_type` 键 → `_compute_staleness()` 用 `.get("knowledge_type", "technical")` 兜底，并以 `created_at` 回算 `expires_at`。**老数据零迁移。**

**总计：改动 2 行，新增 4 个方法 + 3 个键。**

## C4. 新增角色：经验评估员（experience_evaluator）

### C4-1. role_pool.json 条目（可直接粘贴）

```json
{
  "id": "experience_evaluator",
  "name": "经验评估员",
  "group": "general",
  "model": "text",
  "gpu_affinity": "gpu2",
  "capabilities": [
    "experience_scoring",
    "utility_judgement",
    "knowledge_freshness_audit",
    "memory_pruning"
  ],
  "tools": [
    "experience_manager",
    "knowledge_base",
    "memory_query"
  ],
  "description": "任务收尾后为被引用的经验打效用分，审计知识新鲜度，淘汰失效记忆"
}
```

同时需在 `gpu_allocation.gpu2.roles` 数组追加 `"experience_evaluator"`。

### C4-2. prompt 全文（842 字，符合标准执行者档 600-900）

```markdown
# 经验评估员 (Experience Evaluator) · 系统提示词

## 1. 身份
你是「经验评估员」，团队记忆的守夜人。任务结束后，你回答一个问题：**这次用到的经验，到底帮了忙还是帮了倒忙？**

你不执行任务、不写代码、不与用户直接对话。你只在流水线收尾后被调用一次，输出一份评分单。

你存在的理由：不加筛选地累积经验，效果比不存经验更差——过时和错误的经验会污染上下文，让团队照着错的做。你就是那道筛子。

## 2. 输入
每次评估你会拿到三样东西：
- **本次注入的经验清单**（每条含 task_type、成功步骤、当前 utility_score）
- **本次任务的执行日志**（每步成功/失败、失败原因）
- **最终结果**（交付成功 / 部分成功 / 失败）

## 3. 评分规则（严格按此表，不自由发挥）
| 判定 | 分值 | 依据 |
|------|------|------|
| 经验被采用且任务成功 | **+1** | 执行日志中能看到经验里的步骤被实际执行，且结果全绿 |
| 经验被注入但任务失败 | **−1** | 失败原因与该经验无关，但它没能帮上忙 |
| 经验误导了执行 | **−2** | 失败原因**直接指向**经验里的某个步骤（照做了，错了） |
| 经验未被采用 | **0** | 执行日志里找不到对应步骤——不奖不罚 |

判 −2 必须指出是哪一步误导的。指不出来就只能判 −1。

## 4. 知识新鲜度审计
同时检查本次注入的知识三元组：
- 过期的（`staleness ≥ 1.0`）：标记 `建议复核`，并说明"该事实可能已变化"
- 与本次执行事实**矛盾**的：标记 `建议失效`，并给出矛盾点
- 不确定的：**标 `保留`**。宁可留一条可疑的，也不要误删一条正确的

## 5. 边界
- 你不修改经验内容，只打分和标记——修正由记录经验的角色负责
- 你不评价角色表现（那是人事经理的事），只评价**经验本身的效用**
- 你不因为经验来自哪个角色而调整分数
- 证据不足时**判 0，不猜**

## 6. 输出格式
```
## 经验评估单
任务：{task_type}    结果：{成功/部分成功/失败}

### 经验评分
| 经验 | 判定 | 分值 | 依据 |
|------|------|------|------|
| {task_type} | {采用且成功/未帮上/误导/未采用} | {+1/-1/-2/0} | {一句话，指到具体步骤} |

### 知识新鲜度
| 三元组 | 状态 | 建议 |
|--------|------|------|
| {主-谓-宾} | {新鲜/过期/矛盾} | {保留/复核/失效} |

### 淘汰建议
{达到 −3 的经验列出；无则写"无"}
```

## 7. 交付前自查
每条经验都给了分？每个 −2 都指到了具体步骤？证据不足的判了 0 而不是猜？知识审计里"不确定"的都标了保留？
```

### C4-3. 接入位置

在 `master.py` 的工作组流水线收尾处（当前 `_apply_cleanup_hook` 的同一位置——**方案 B 刚好会腾空这个钩子点**）追加一步：若本次执行注入过经验，则调用 `experience_evaluator`，把评分单回写给 `ExperienceManager.vote()`。

> 这形成一个漂亮的闭环：方案 B 移走 cleaner 让出钩子位，方案 C 用 experience_evaluator 填上。**流水线步数不增加。**

## C5. 演示视频演出脚本（这是拿创意分的关键 90 秒）

> 目标：让评委在 90 秒内**看见记忆在自我进化**，而不是听我们说它会。

| 时间 | 画面 | 旁白要点 |
|------|------|---------|
| 0-15s | 分屏：左侧对话框，右侧 `data/experiences/` 的 JSON 实时刷新 | "我们的团队有记忆。但记忆本身也需要被管理。" |
| 15-35s | **第一次**提同一个任务，团队跌跌撞撞完成，经验被记录，`utility_score: 0` | "第一次做，它把成功路径记了下来。" |
| 35-55s | **第二次**提相似任务，经验被注入（高亮显示注入的上下文块），一次通过。评估员打 **+1**，JSON 里分数跳到 1.0 | "第二次，它照着自己的经验走，一次过。评估员给了 +1。" |
| 55-75s | **手动篡改**一条经验为错误步骤，再跑一次 → 任务失败 → 评估员判 **−2** 并指出是哪一步误导 → 分数跌到 −2 | "关键在这——我们故意塞了一条错经验。它失败了，但它**知道是哪一步坑了自己**。" |
| 75-85s | 再跑一次，该经验降到 −3，**自动从注入池消失**，团队恢复正常 | "−3 出局。坏记忆会被自己淘汰掉。" |
| 85-90s | 切到知识库面板，一条标着"⚠ 可能已过期"的三元组 | "知识也一样——有保质期。" |

**演出要点**：第 55-75 秒的"主动投毒"是整段的戏眼。它把一个抽象的架构设计变成了**可证伪的现场实验**，评委看得懂、记得住。这是 Harvard 论文结论在 20 秒内的可视化。

## C6. 风险

| 风险 | 等级 | 缓解 |
|------|------|------|
| 新增字段破坏老数据加载 | **低** | `from_dict()` 已有字段过滤；`.get()` 兜底；已读源码确认 |
| 评估员多一步推理，拖慢流水线 | 中 | 占用方案 B 腾出的 cleaner 位，**步数不增**；且该角色 prompt 仅 842 字 |
| 评估员判分不稳定（同样情况给不同分） | 中 | prompt 里评分表写死为查表，禁止自由发挥；"证据不足判 0 不猜" |
| TTL 误杀有效知识 | 低 | 过期只**降权**不删除，`sweep_expired()` 需显式调用；审计里"不确定→保留" |
| 演示时投毒环节翻车 | 中 | 脚本第 55-75 秒需提前录制备份镜头 |

## C7. 方案 C 结论卡

| 项 | 内容 |
|----|------|
| 预估工时 | **10 小时**（C-1 经验评分 3h + C-2 知识 TTL 3h + 评估员角色 1.5h + 演示面板 1.5h + 联调 1h） |
| 风险等级 | **低-中**（纯加法，老数据零迁移，唯一的"改"是 4 行排序/加权表达式） |
| 最小可交付版本 | **只做 C-1 经验效用评分 + 评估员角色**（约 4.5h）。演示脚本的 0-85 秒全部可演，只砍掉最后 5 秒的知识 TTL 镜头。**这是保底方案** |
| 对应评分 | 核心能力 **20 分**（记忆系统是 DevTeam 架构的主角）+ 创意 **20 分**（经验自我淘汰 + 现场投毒演示，是全场最容易被记住的设计） |
| 建议次序 | **最后执行，但优先级最高**——它是唯一能同时拿两个 20 分的项 |

---

# 三方案总览

| | 方案 A Prompt 精简 | 方案 B 裁撤角色 | 方案 C 记忆补齐 |
|---|---|---|---|
| 工时 | 8h | 2.5h | 10h |
| 最小版本工时 | 3.5h | 0.7h | 4.5h |
| 风险 | 中 | 低-中 | 低-中 |
| 可逆性 | 完全可逆（保留原文件） | 完全可逆（归档非删除） | 完全可逆（纯加法） |
| 对应分数 | 速度 20 | 架构合理性（间接） | 核心能力 20 + 创意 20 |
| 依赖关系 | 依赖 B 先做（减 6 份工作量） | 无依赖，**应最先做** | 依赖 B 腾出钩子位（非强制） |

## 建议执行次序与时间账

```
B（2.5h）──▶ A（8h）──▶ C（10h）        总计 20.5h
 └─ 最小版 0.7h ─▶ 最小版 3.5h ─▶ 最小版 4.5h    保底 8.7h
```

**若时间紧张，按此优先级砍**：
1. 先保 **C 的最小版**（4.5h）—— 唯一能拿两个 20 分的
2. 再保 **B 的最小版**（0.7h）—— 性价比最高，40 分钟换一整步流水线
3. **A 的最小版**（3.5h）—— 速度分靠它，但没有 C 值钱
4. 剩余时间才做三者的完整版

## 执行前必须获得的确认

本文档三项改造**均未执行**。执行前需用户逐项确认：

- [ ] 方案 A：是否采纳"保留原文件 + slim 变体 + 环境变量切换"的 A/B 方式？
- [ ] 方案 B：是否接受"L2 归档而非物理删除"？是否同意删掉 4 条工作组？
- [ ] 方案 C：是否接受新增 `experience_evaluator` 角色（角色数 17 → 12，含新增）？
- [ ] 执行次序：是否按 B → A → C？
- [ ] 时间盒：是否按"最小版优先"策略（保底 8.7h）？
