# MyAgent 参赛演示视频 · 分镜脚本

> 赛事：2026 AMD AI DevMaster 黑客松 · 赛道二（私有 AI Agent 本地部署）
> 目标时长：**4 分 15 秒**（要求 3–5 分钟）
> 录制环境：单卡 AMD Radeon PRO W7900（48GB / gfx1100 / ROCm）云实例
> 版本状态：**随时可录（Ready-to-shoot）** — 见文末「当前状态说明」

---

## 一、评分锚点与镜头映射

| 官方评分项 | 分值 | 对应镜头 |
|---|---:|---|
| 任务定位清晰 | 20 | S1 开场、S8 收尾 |
| 核心能力（分解/工具/RAG/记忆） | 20 | **S3 工具落盘**、**S4 记忆隔离**、S5 多角色流水线 |
| 多轮交互流畅度 | 20 | **S4 角色切换多轮**、S5 流水线、S6 工作台 |
| AMD 平台本地推理 | 20 | **S2 GPU 利用率**、S1 硬件亮相、S8 无远程 API 审计 |
| 推理速度优化 | 20 | **S7 A/B 测速（数据待补）** |

★ = 赛事吃分**必拍动作**，缺一不可。

---

## 二、录制前准备清单

### A. 实例与服务（缺一不可）

- [ ] 已拿到 **AMD Radeon PRO W7900 单卡实例**，`rocm-smi` 能列出 GPU
- [ ] `bash setup_amd_cloud.sh` 已跑完（生成 `start_llama.sh`、`bench_helpers.sh`）
- [ ] `cd ~/myagent && bash start.sh` 已跑完，三项健康检查全绿：
  ```bash
  curl -s http://localhost:8000/v1/models   # llama-server
  curl -s http://localhost:8080/api/health  # FastAPI
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost/   # nginx → 200
  ```
- [ ] **rc-tunnel 暴露前端**（`start.sh` 已固化；隧道空闲 60 秒回收，**开录前务必重跑一次**）：
  ```bash
  rc-tunnel expose --port 80        # 命令不存在时先 /var/run/secrets/frp-self-service/install
  ```
- [ ] 确认单卡路由：后端启动日志有 `[RoleLoader] 推理路由: ...`，且 `pgrep -a llama-server` **只有一个进程**

### B. 数据与素材（必须提前跑完，镜头前不现跑）

- [ ] **A/B 测速已采集完毕**（S7 用）：
  ```bash
  cd ~/myagent && bash stop.sh
  CTX_SIZE=16384 bash start_llama.sh > /tmp/llama.log 2>&1 &   # ⚠ 8192 对 A 组原版 prompt 偏小，会截断
  sleep 60 && grep -m1 "n_ctx" /tmp/llama.log                   # 预期 n_ctx = 16384
  cd ~/myagent/backend && source venv/bin/activate && nohup uvicorn main:app --host 0.0.0.0 --port 8080 > /tmp/backend.log 2>&1 &
  cd ~/myagent && source bench_helpers.sh
  benchmark_orig && for i in 1 2 3 4 5 6; do bench_one "开发一个带增删改查的待办事项 Web 应用"; done
  benchmark_slim && for i in 1 2 3 4 5 6; do bench_one "开发一个带增删改查的待办事项 Web 应用"; done
  bench_report      # 确认 /tmp/bench_original.csv 与 /tmp/bench_slim.csv 都有数据
  ```
  采完立刻 `cp /tmp/bench_*.csv ~/myagent/ && git add -A && git commit -m "data: cloud benchmark" && git push`
- [ ] **预建 2–3 个智能体**（S4 用，`data/agents/` 默认只有 `general_assistant`）：
  ```bash
  curl -s -X POST http://localhost:8080/api/agents -H "Content-Type: application/json" \
    -d '{"agent_id":"demo_writer","name":"写作助手","description":"负责文案与报告"}'
  curl -s -X POST http://localhost:8080/api/agents -H "Content-Type: application/json" \
    -d '{"agent_id":"demo_dev","name":"开发助手","description":"负责编码与工程任务"}'
  curl -s http://localhost:8080/api/agents      # 确认列表里有 3 个
  ```
- [ ] **预生成 2–3 个工作台模块**（S6 用；AI 现场生成要等 LLM，镜头里只做「添加 + 拖拽 + 保存」）：
  提前在 `/workbench` → 编辑布局 → 添加模块 → AI 生成，跑出 2–3 个模块存入 `data/modules/`
- [ ] **清场演示文件**（S3 要先展示「文件不存在」）：
  ```bash
  rm -rf ~/myagent/backend/workspace_demo
  ```

### C. 画面与录制

- [ ] 分屏固定：**左 = 浏览器（rc-tunnel 公网地址）**，**右 = 实例终端**
- [ ] 右侧终端预留一个窗口跑实时监控：
  ```bash
  watch -n 1 'rocm-smi --showuse --showmeminfo vram | head -20'
  ```
- [ ] 再备一个终端窗口，`cd ~/myagent` 待命（S3 落盘验证用）
- [ ] 终端字号调大（`Ctrl + Shift + +`），关闭所有无关窗口与私人信息
- [ ] 录屏软件确认在录**声音**（若配真人旁白）
- [ ] **完整彩排一遍**再正式录（LLM 输出有随机性，S3/S5 尤其要练）

---

## 三、分镜表

> 时间为累计时间轴；「预期屏幕结果」是验收标准，达不到就重录该镜头。

### S1 · 开场与硬件亮相（0:00–0:20，20 秒）

| 项 | 内容 |
|---|---|
| **画面** | 全屏终端。片头一张标题卡：`MyAgent · 私有 AI Agent 本地部署 · AMD Radeon PRO W7900` |
| **操作** | 终端执行：<br>`rocminfo \| grep -m1 "Marketing Name"`<br>`rocm-smi --showmeminfo vram --csv \| awk -F, 'NR>1 && $2 ~ /^[0-9]+$/ {printf "VRAM: %.1f GB\n", $2/1073741824}'`<br>`pgrep -a llama-server` |
| **旁白** | 「MyAgent 是一套完全私有化部署的多角色 AI 团队。全部推理跑在这一张 AMD Radeon PRO W7900 上，48GB 显存，llama.cpp + ROCm 本地推理，不调用任何云端 API。」 |
| **预期屏幕结果** | 输出 `Marketing Name: ... Radeon PRO W7900`；`VRAM: 47.x GB`；`pgrep` **只列出一行** llama-server 进程（证明单卡单实例） |

---

### S2 · ★ 单卡 GPU 本地推理（0:20–0:50，30 秒）

| 项 | 内容 |
|---|---|
| **画面** | 分屏：左浏览器（对话视图），右终端 `watch -n 1 rocm-smi ...` 实时刷新 |
| **操作** | 在左侧对话框发送：`你好，用一句话介绍你自己`，发送后镜头**不切走**，让观众看到右侧 GPU 曲线与回复同时发生 |
| **旁白** | 「提问的这一瞬间，注意右边——GPU 利用率直接冲起来，显存占用十一到十三 GB。这是本机这张卡在算，不是在转发远程接口。」 |
| **预期屏幕结果** | 左：流式回复正常输出；右：`GPU use (%)` 从 0 冲到 **60–100%**，`VRAM` 约 **11–13 GB**；回复结束后回落。<br>❌ 若 GPU 恒 0%、VRAM 仅几百 MB → 模型跑在 CPU 上，**停止录制先修环境** |

---

### S3 · ★ 工具调用真实落盘（0:50–1:30，40 秒）

> **本镜头是「不是假回复」的硬证据，必须一镜到底不剪辑。**

| 项 | 内容 |
|---|---|
| **画面** | 分屏：左浏览器对话，右终端（`cd ~/myagent`） |
| **操作** | ① 先在右侧终端**证明文件不存在**：<br>`date; ls -l ~/myagent/backend/workspace_demo/ 2>&1`<br>② 左侧对话框发送：<br>`请用 file_write 工具，在 workspace_demo/demo_todo.md 创建一个待办清单文件，内容包含三条今日任务。`<br>③ 等回复出现工具调用结果后，右侧终端**同一个窗口**执行：<br>`ls -l ~/myagent/backend/workspace_demo/`<br>`cat ~/myagent/backend/workspace_demo/demo_todo.md`<br>`stat -c '%n  修改时间: %y' ~/myagent/backend/workspace_demo/demo_todo.md` |
| **旁白** | 「先看清楚，这个目录现在是空的、文件不存在。现在让它用 file_write 工具写一个文件……工具执行完，回到同一个终端 ——文件真的在磁盘上，内容就是它刚生成的，时间戳是刚才这一秒。这不是模型编出来的一段文本，是真实的文件系统写入。」 |
| **预期屏幕结果** | ① 第一次 `ls` 报 `No such file or directory`；③ 第二次 `ls` 列出 `demo_todo.md`，`cat` 打印出与对话内容一致的清单，`stat` 的修改时间与画面左上角 `date` 时间**相差数秒内** |
| **注意** | `file_write` 有安全约束（`backend/core/tools/builtin/file_tools.py::_is_within_project`），**只允许写项目目录内的相对路径**；后端进程工作目录为 `backend/`，故产物落在 `~/myagent/backend/workspace_demo/`。写绝对路径或 `../` 会被拒绝，不要在镜头前试。 |

---

### S4 · ★ 角色切换与记忆隔离（1:30–2:15，45 秒）

> 两段证据：**前端可见的会话隔离** + **磁盘可见的角色级隔离**。二者都拍，不要混为一谈。

| 项 | 内容 |
|---|---|
| **画面** | 左浏览器（对话视图左侧智能体侧边栏），右终端 |
| **操作** | ① 选中 **通用助手** → 发送：`记住，我叫小林，我的技术栈是 Rust。`<br>② 点侧边栏切到 **写作助手** → 发送：`我叫什么名字？我的技术栈是什么？`<br>③ 再切到 **开发助手** → 发送：`我们之前聊过什么吗？`<br>④ 切回 **通用助手** → 发送：`我的技术栈是什么？`<br>⑤ 右侧终端展示角色级隔离落盘：<br>`ls ~/myagent/data/memory/archive/`<br>`ls -l ~/myagent/data/memory/sessions/` |
| **旁白** | 「切换角色。我先告诉通用助手我叫小林、用 Rust。切到写作助手问同样的问题——它不知道，因为它根本看不到那段上下文。开发助手也一样。再切回来，通用助手记得清清楚楚。<br>底层是四级渐进记忆做的目录级隔离：每个角色一套独立的归档目录和会话文件，角色之间只能通过带访问控制的共享黑板交换信息，不存在上下文串味。」 |
| **预期屏幕结果** | ② ③ 两个助手明确回答「不知道 / 没有相关记录」，**不得**说出「小林」或「Rust」；④ 通用助手准确复述「小林 / Rust」；⑤ 终端列出 18 个以角色命名的独立归档目录（`coach/ designer/ developer/ ...`）与 `sessions/{role}.json` |
| **诚实口径** | 侧边栏切的是**智能体（Agent）会话**；终端展示的是**内部 19 角色**的记忆目录（`role_pool.json` 声明 18 个可分发角色 + 常驻 Secretary = 19）。旁白按此表述，**不要**说「侧边栏里有 19 个角色」。 |

---

### S5 · 多角色流水线（2:15–2:55，40 秒 · 中段可倍速）

| 项 | 内容 |
|---|---|
| **画面** | 左浏览器对话（跟随流水线滚动），右终端保持 `rocm-smi` 监控 |
| **操作** | 左侧发送：`开发一个带增删改查的待办事项 Web 应用`<br>触发 `dev_full` 工作组，让画面跟着角色标签逐个出现 |
| **旁白** | 「一句话触发完整开发流水线：教练拆需求、设计师出方案、开发写代码、巡检审架构、测试、部署、清洁员收尾，最后经验评估员给这次运行的经验打分。八个角色，各自独立人格，全程在这一张卡上顺序执行——所以 48GB 就能承载整个团队。」 |
| **预期屏幕结果** | 消息流依次出现 `coach → designer → coach → developer → inspector → tester → deployer → cleaner` 角色标签；右侧 GPU 利用率**全程维持高位**（证明每一步都在本地推理） |
| **剪辑** | 若超过 90 秒未跑完：**保留首尾，中段 4–8 倍速**并打字幕「流水线中段已加速」。**不得**剪掉角色标签导致看起来是一次调用。 |

---

### S6 · ★ 工作台看产物（2:55–3:25，30 秒）

| 项 | 内容 |
|---|---|
| **画面** | 浏览器全屏，顶部导航切到「工作台」 |
| **操作** | ① 点导航「工作台」→ 展示已有模块面板<br>② 点「编辑布局」→「+ 添加模块」→ 从模块库添加一个**预生成**的 AI 产物模块<br>③ **鼠标拖拽**两个面板交换位置（GridStack 拖拽动画要拍清楚）<br>④ 点「保存布局」<br>⑤（可选 3 秒）切到「皮肤」视图一闪而过，点一个皮肤即时换肤 |
| **旁白** | 「AI 生成的产物不是只躺在聊天记录里。工作台是可拖拽的 GridStack 面板，每一个卡片都是一个模块化产物，位置可以随手调整并保存成个人布局。加上皮肤市场，一共三个视图：对话、工作台、皮肤。」 |
| **预期屏幕结果** | 顶部显示「N 个模块」计数；拖拽时面板跟手且发生**位置互换**；点「保存布局」后有保存反馈，刷新页面布局仍在（布局落盘 `data/layouts/`） |
| **注意** | 模块 **AI 生成需等 LLM，镜头里不现跑**；使用准备清单 B 中预生成的模块。若旁白提到「AI 生成」，须同时说明「这些模块此前由 AI 生成并已入库」。 |

---

### S7 · ★ AMD 推理速度优化 A/B 测速（3:25–3:55，30 秒）

> ⚠️ **本镜头数据状态：PENDING。**
> **此处展示实测 TTFT / 吞吐，数据待补** —— 云实例排队中，尚未拿到实测值。
> **录制时必须使用 `bench_report` 打印出来的真实 CSV 数字，严禁在旁白、字幕或图表中填写任何未实测的数值。**

| 项 | 内容 |
|---|---|
| **画面** | 终端全屏（字号加大），必要时叠加一张对比表字幕卡 |
| **操作** | ① `grep -m1 "n_ctx" /tmp/llama.log`（证明 A/B 同一上下文长度）<br>② `cd ~/myagent && source bench_helpers.sh && bench_report`<br>③ 光标依次指向 `original` 与 `slim` 两组的 `elapsed_ms` / `prompt_tokens` 与平均值 |
| **旁白（模板，X/Y/Z 待实测填入）** | 「速度优化这块我们做了严格 A/B：**同一台机器、同一个 Qwen2.5-14B-Q4_K_M、同一个输入，唯一变量是角色 prompt 版本**，用环境变量 `PROMPT_VARIANT` 秒切，不改一行代码。原版基线对比精简版——prefill 词元从 X 降到 Y，端到端耗时降低 Z%。这些是 llama-server 返回的真实 `prompt_tokens`，不是估算。」 |
| **预期屏幕结果** | 终端打印 `--- original ---` / `--- slim ---` 两段 CSV（`variant,elapsed_ms,prompt_tokens,timestamp`）与各自「平均耗时」行；两组行数一致、时间戳为本次实例采集 |
| **数据纪律** | ① 丢弃每组**第 1 次**（冷启动）；② 取中位数写入提交材料；③ 本地曾估算的「−59.9% 字数 / 约 −83% prefill」是**估计值，不得作为测速结果播报**；④ 若 `bench_report` 为空或只有一组 → **本镜头不得录制**，改用降级方案（见第四节） |

---

### S8 · 收尾与本地化承诺（3:55–4:15，20 秒）

| 项 | 内容 |
|---|---|
| **画面** | 左：`docs/architecture.png` 架构图；右：终端 |
| **操作** | 终端执行（可选，3 秒定格）：<br>`grep -n "assert_local_endpoint" ~/myagent/backend/config/settings.py \| head -3`<br>然后切架构图定格，叠加结尾字幕卡 |
| **旁白** | 「一句话总结：19 个角色、四级记忆、真实工具落盘，全部跑在一张 AMD Radeon PRO W7900 上。代码里没有任何远程 API 路径——任何非本地推理地址都会在启动时被硬性拒绝。数据不出机器，这就是私有 AI Agent。」 |
| **预期屏幕结果** | 终端命中 `assert_local_endpoint()` 所在行；结尾字幕卡：`MyAgent · 100% 本地推理 · AMD Radeon PRO W7900 · llama.cpp + ROCm` |

---

## 四、必拍动作核对表（交片前逐条打勾）

| # | 必拍动作 | 镜头 | 验收标准 |
|---|---|---|---|
| 1 | **工具调用真实落盘** | S3 | 同一终端窗口内「先无 → 后有」，`cat` 内容匹配 + `stat` 时间戳吻合 |
| 2 | **角色切换 / 记忆无污染** | S4 | 切换后另一助手**明确答不知道**，切回后**准确复述**；终端展示按角色分离的记忆目录 |
| 3 | **工作台看产物** | S6 | GridStack 拖拽互换 + 保存布局，刷新后仍在 |
| 4 | **单卡 GPU 推理** | S1 / S2 | `rocm-smi` GPU 利用率随推理冲高；`pgrep -a llama-server` 仅一个进程 |
| 5 | **AMD 推理优化 A/B 测速** | S7 | `bench_report` 打印 orig 与 slim 两组真实 CSV（**数据 PENDING，待补**） |

### S7 降级方案（仅在实测数据确实未采集到时使用）

若录制窗口内无法完成 A/B 采集，S7 改为**机制演示**，且必须同时满足：

1. 只演示切换机制，不给任何数字：
   ```bash
   type benchmark_slim | head -1        # benchmark_slim is a function
   grep -n "PROMPT_VARIANT" ~/myagent/backend/core/agent/loader.py | head -3
   wc -c ~/myagent/domain/roles/*/prompt.txt ~/myagent/domain/roles/*/prompt.slim.txt 2>/dev/null | tail -3
   ```
2. 旁白改为：「A/B 开关已在代码内，`PROMPT_VARIANT` 一个环境变量秒切原版与精简版，无需改代码。**实测 TTFT 与吞吐数据将随提交材料一并给出。**」
3. 画面必须打字幕：**「实测数据采集中，以提交材料为准」**
4. **绝不允许**用字数降幅、估算词元或任何未实测数字冒充测速结果。

---

## 五、录制完成后立即执行

```bash
cd ~/myagent
cp /tmp/bench_*.csv . && cp /tmp/llama.log ./llama_demo.log
git add -A && git commit -m "data: cloud benchmark + demo logs" && git push
```

- 用 `bench_report` 的中位数回填 `CLOUD_BENCHMARK.md` 第 8 节报告模板与 `docs/项目说明文档.md` 6.4 节的「[实测数值待补]」占位。
- **实例销毁前，确认视频文件已下载到本地电脑。**
- 演示中产生的临时文件清理：`rm -rf ~/myagent/backend/workspace_demo`

---

## 六、当前状态说明

> **本脚本为「随时可录（Ready-to-shoot）」状态。**
>
> 截至撰写时，**AMD Radeon 云实例仍在排队、暂不可用**，因此：
>
> - S7 的 **A/B 实测 TTFT / 吞吐 / prompt_tokens 数据为 `PENDING`**，脚本中一律以占位符 X / Y / Z 表示，**未填入任何数字**；
> - 所有镜头的操作命令、验收标准均已按仓库现有代码与脚本（`setup_amd_cloud.sh`、`start.sh`、`bench_helpers.sh`、`QUICKSTART_CLOUD.md`、`CLOUD_BENCHMARK.md`）核对，可直接照做；
> - **一旦拿到实例**：按第二节准备清单逐条打勾 → 先完成 A/B 数据采集并 `git push` → 再按 S1–S8 顺序录制 → 用实测数字回填 S7 旁白与提交材料。
>
> 数据诚信底线：**宁可标注「数据待补」，也绝不编造任何一个数字。**
