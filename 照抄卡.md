# 上机照抄卡（小白版 · 一屏搞定）

> 排到实例后就看这一张。**每个方框整块复制，粘贴进去，按回车，等它跑完。**
> 出问题了不要慌，直接把屏幕截图发给我。
> 详细版在 `QUICKSTART_CLOUD.md`，这张卡搞不定时再翻那份。

---

## 怎么粘贴命令（先看这个）

- 复制方框里的全部内容（包括换行）
- 在网页终端里点一下，然后按 **Ctrl + Shift + V** 粘贴（Linux 终端不是 Ctrl+V）
- 粘贴完按 **回车**
- 屏幕开始滚字 = 正常，**别按任何键，等它停**

---

## 第 1 块：确认机器对不对（30 秒）

```bash
echo "=== 显卡型号 ===" && rocminfo | grep -m1 "Marketing Name"
echo "=== 显存大小 ===" && rocm-smi --showmeminfo vram --csv | awk -F, 'NR>1 && $2 ~ /^[0-9]+$/ {printf "%.1f GB\n", $2/1073741824}'
echo "=== ROCm 版本 ===" && cat /opt/rocm/.info/version 2>/dev/null
```

**看到这样就对了：**
```
=== 显卡型号 ===
  Marketing Name:    AMD Radeon PRO W7900
=== 显存大小 ===
48.0 GB
=== ROCm 版本 ===
7.2.0
```

| 显存数字 | 怎么办 |
|---|---|
| **45 GB 以上** | ✅ 完美，往下走 |
| 30–45 GB | ⚠️ 也能跑，往下走就行，别改参数 |
| **不到 30 GB** | ❌ **停下来，截图发我**，别继续 |

---

## 第 2 块：把代码弄上去（2 分钟）

**先决条件**：你已经双击跑过 `push_to_github.bat`，代码在 GitHub 上了。

把下面这行里的 `你的仓库地址` 换成真的地址（就是 bat 文件最后打印的那个）：

```bash
cd ~ && git clone 你的仓库地址 myagent && cd ~/myagent && ls setup_amd_cloud.sh start.sh
```

**看到 `setup_amd_cloud.sh  start.sh` 两个名字 = 成功。**

> 如果 GitHub 那步没搞定，也可以：在 Radeon Cloud 的 JupyterLab 界面里
> 直接把本地文件夹压缩成 zip 拖上去，然后跑
> `cd ~ && unzip MyAgent-main.zip -d myagent && cd ~/myagent`

---

## 第 3 块：一键部署（最慢，40 分钟左右）

**先装个保险**（防止网断了前功尽弃）：

```bash
sudo apt-get update -qq && sudo apt-get install -y tmux && tmux new -s work
```

屏幕会变一下，底部出现一条绿条 —— 这是正常的，说明保险生效了。**从现在起断网也不怕。**

然后跑部署：

```bash
cd ~/myagent && bash setup_amd_cloud.sh 2>&1 | tee ~/setup.log
```

### ⏳ 接下来 40 分钟会发生什么

| 阶段 | 屏幕表现 | 时长 |
|---|---|---|
| Step 1-3 | 快速滚字 | 2 分钟 |
| **Step 4 编译** | **看着像卡死了** | **10–20 分钟** |
| **Step 5 下模型** | 进度条慢慢爬 | **5–30 分钟** |
| Step 6-9 | 又开始滚字 | 3 分钟 |

**⚠️ Step 4 屏幕不动是正常的，千万别按 Ctrl+C。** 去泡杯茶。

**跑完会看到：**
```
============================================
  部署完成！
============================================
```

---

## 第 4 块：启动 + 检查（3 分钟）

```bash
cd ~/myagent && bash start.sh
```

等它自己跑完（约 1 分钟，模型加载慢），然后检查：

```bash
echo "--- 推理引擎 ---"; curl -s http://localhost:8000/v1/models | head -c 100
echo; echo "--- 后端 ---"; curl -s http://localhost:8080/api/health
echo; echo "--- 网页 ---"; curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost/
```

**三行都要有正常输出：**
```
--- 推理引擎 ---
{"object":"list","data":[{"id":"Qwen2.5-14B-Instruct"...
--- 后端 ---
{"status":"ok"...
--- 网页 ---
HTTP 200
```

**有任何一行是空的或报错 → 截图发我。**

---

## 第 5 块：拿到网址，用浏览器打开（1 分钟）

```bash
echo "在你自己电脑的浏览器打开这个地址："; echo "http://$(curl -s ifconfig.me)/"
```

复制打印出来的网址，**在你自己电脑的浏览器里打开**，应该能看到界面并且能聊天。

> 打不开但上一步 HTTP 200 是正常的 → 云平台防火墙没放行。
> 去 Radeon Cloud 控制台找「安全组 / 端口」，加一条 **80 端口** 放行。搞不定截图发我。

---

## 🎉 到这里就算部署成功了

**立刻做一件事：截图 + 把这句话跑一遍存证**

```bash
{
  echo "=== 部署成功存证 $(date) ==="
  rocminfo | grep -m1 "Marketing Name"
  rocm-smi --showmeminfo vram --csv | awk -F, 'NR>1 && $2 ~ /^[0-9]+$/ {printf "VRAM: %.1f GB\n", $2/1073741824}'
  grep -m1 "n_ctx" /tmp/llama.log
  grep -m1 -i "offloaded" /tmp/llama.log
} | tee ~/myagent/baseline_info.txt
```

**然后告诉我一声，我给你下一步的卡片（测速 + 录视频）。**

---

## 🆘 出问题了怎么办

### 万能第一招：全部重启

```bash
cd ~/myagent && bash stop.sh; pkill -f llama-server; pkill -f uvicorn; sleep 3; bash start.sh
```

八成的问题这一下就好了。

### 屏幕不动了，不知道死没死

**另开一个终端**（JupyterLab 里点 `+` 新建 Terminal），跑：

```bash
tail -20 ~/setup.log
```

有新内容在冒 = 活着，继续等。

### SSH 断了 / 网页刷新了

```bash
tmux attach -t work
```

刚才跑的东西还在。

### 什么都不对，想从头再来

```bash
cd ~/myagent && bash stop.sh; pkill -f llama-server; pkill -f uvicorn; sudo pkill nginx
rm -rf ~/llama.cpp/build ~/myagent/backend/venv
cd ~/myagent && bash setup_amd_cloud.sh 2>&1 | tee ~/setup.log
```

（模型不会重下，放心）

### 要看日志

| 出了什么事 | 跑这条 |
|---|---|
| 部署失败 | `grep -iE "error\|failed" ~/setup.log \| head -20` |
| 聊天没反应 | `tail -30 /tmp/backend.log` |
| 回复特别慢 | `tail -30 /tmp/llama.log` |

**把输出截图发我，我来看。**

---

## 📌 最重要的三条纪律

1. **实例别随便销毁** —— 销毁了要重新排队，所有东西都没了
2. **数据要及时存** —— 测速结果、截图，随时拷回本地
3. **卡住超过 10 分钟就问我**，别自己死磕
