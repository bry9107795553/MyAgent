# 项目简报 · AMD AI DevMaster 黑客松 赛道二

> 由前台接待产出，交接给**项目接手员**
> 产出时间：2026-08-04

---

## 一、基本信息

| 字段 | 内容 |
|------|------|
| **项目名称** | MyAgent —— 基于 AMD Radeon GPU + ROCm 的私有 AI Agent 平台，改造为 DevTeam 多角色协作架构 |
| **项目性质** | **存量改造**（已有 13,271 行 Python，54 个文件），非新建 |
| **用户水平** | **小白** —— 命令行/环境配置需要代劳，需提供可直接执行的现成产物 |
| **时间约束** | 🔴 **2026-08-06 23:59（北京时间）截止，剩余约 60 小时** |
| **项目路径** | `G:\workburddy\workspace\MyAgent-main` |
| **版权归属** | ✅ 用户自有，可直接参赛 |

---

## 二、赛事要求（已消化）

**赛事**：2026 AMD AI DevMaster 黑客松 · 赛道二「私有 AI Agent 开发与本地部署」
**奖金**：一等奖 $5,000 / 二等奖 $3,500 / 三等奖 $1,500

### 评分表（100 分）

| 维度 | 分值 | 细项 |
|------|------|------|
| AI Agent 功能完整性 | **60** | 任务定位与创意场景 20 / 核心能力（任务分解·工具调用·RAG·记忆）20 / 多轮交互流畅度 20 |
| AMD Radeon GPU 与 ROCm 适配 | **40** | 核心推理跑在 Radeon GPU 上 20 / 推理速度定向优化 20 |

### 硬性技术约束

- ✅ 必须运行在 AMD Radeon GPU + ROCm 软件栈
- 🔴 **核心推理不允许使用远程 API**
- 🔴 **不允许完全依赖闭源 Agent 平台实现核心功能**
- 最低功能要求：RAG / 工具调用 / 多步骤规划 / 本地多轮记忆 / 权限隐私控制，**5 选 2**（多做加分）

### 提交要求（4 项，缺一不可）

1. **项目说明文档** —— 应用场景 / Agent 架构图 / 核心能力 / 模型与本地部署方案 / **GPU 推理速度优化说明**
2. **项目源代码** —— 完整仓库 + README（环境配置、启动指南、依赖列表）
3. **演示视频** —— 3–5 分钟，展示实际操作流程 + 在 Radeon GPU 上的真实执行表现
4. **补充材料** —— PPT 或海报（二选一）

### 提交方式（易踩坑）

- Fork `AMD-DEV-CONTEST/Radeon-hackathon-2026-07` → 提 Pull Request
- PR 标题格式：`Track 2, <姓名或队名>, <应用名>`
- 🔴 **所有提交材料、项目说明、PR 内容必须为英文**

---

## 三、GPU 资源现状

**平台**：Radeon Cloud — https://radeon-global.anruicloud.com/

用户状态：Luma 已报名 ✅ / AMD 开发者计划已注册 ✅ / Radeon Cloud 可登录 ✅

🔴 **核心瓶颈：实例需排队，且很难排到。**

### 由此推导的强制策略

> **离线开发 + 上机一次成型。**
> 所有代码改造、配置替换、流程调试必须在本地完成。一旦排到实例，按预先写好的剧本一次性完成：部署 → 验证 → 压测取数 → 录视频。**不允许把调试留到实例上做。**

### 平台能力备忘

- JupyterLab 网页终端 / SSH 两种接入
- 支持自建 vLLM 服务，暴露 OpenAI 兼容端点
- `rc-tunnel` 可将本地端口映射为公网 URL（`rc-<random>.radeon.firstdg.ai`），**录视频演示可用**
- 实例按额度计费，用完必须手动 Destroy
- ⚠️ 平台提供的「免费共享 Model API」（Qwen/DeepSeek）**严禁用作核心推理** —— 违反赛道规则

---

## 四、项目现状诊断（前台初勘）

### 已有资产

| 项目 | 状态 |
|------|------|
| 后端 | FastAPI + WebSocket，13,271 行 Python / 54 文件 |
| 前端 | Vue 3 + Vite + GridStack.js |
| 推理层 | llama.cpp + ROCm，Qwen2.5-14B-Instruct-Q4_K_M (GGUF) |
| 部署脚本 | `setup_amd_cloud.sh` 完整 8 步：ROCm 编译 llama.cpp → 下模型 → 配后端 → 构建前端 → 生成启动脚本 |
| 角色系统 | **配置驱动** — `data/role_pool.json`（16 角色）+ `data/workgroups/*.json`（10 条流水线）+ `backend/core/role/loader.py` |
| Agent 配置 | 目录隔离 `data/agents/{id}/config.yaml + prompt.txt + ui_layout.json` |
| 记忆系统 | 4 级渐进记忆 + 共享黑板（`backend/core/memory/blackboard.py`） |
| 文档 | 英文 README 299 行 + PROJECT_SPEC.md 197 行 |
| 测试 | `tests/offline_test.sh`、`tests/test_secretary.py` |

### 🔴 风险清单（按优先级）

| # | 风险 | 说明 | 影响 |
|---|------|------|------|
| 1 | **从未成功运行过** | 13,271 行代码零验证，本地和云上都没跑通 | 🔴 致命 — 60 小时内必须先跑通 |
| 2 | **GPU 实例排队** | 云上验证窗口不可控 | 🔴 致命 — 决定开发策略 |
| 3 | **部署脚本目标环境错配** | 脚本头部写 "ModelScope AMD 实例 / 192GB VRAM"（MI300X 级别），实际 Radeon Cloud 显存远小于此。`-ngl 99 --ctx-size 32768 --parallel 4` 参数需按实际显存重算 | 🟡 高 |
| 4 | **保留云端 API 降级通道** | `.env` 含 `ZHIPU_API_KEY` / `CLOUD_API_ENABLED`，虽默认关闭，但违反"核心功能禁用远程 API"的观感 | 🟡 高 — 建议彻底移除 |
| 5 | **README Team 信息为占位符** | 第 294 行仍是 `[Team Member 1] \| [Role] \| [Email]` | 🟡 中 — 提交前必填 |
| 6 | **缺推理速度优化说明** | 评分表中 20 分直接对应此项，需实测数据（tokens/s、首 token 延迟、显存占用） | 🟡 高 — 20 分 |
| 7 | **演示视频未开始** | 3–5 分钟，需真实 Radeon GPU 执行画面 | 🟡 高 — 必交项 |
| 8 | **架构图 / PPT 未产出** | 提交要求第 1、4 项 | 🟢 中 — 出口阶段由前台承接 |

---

## 五、用户核心诉求

> 原话：「我觉得 MyAgent 是不是做得太重了，没有 DevTeam 轻便，而且性能集中。能不能将 MyAgent 改造成 DevTeam，或者融合以 DevTeam 为主呢？」

**用户选定方向**：以 MyAgent 为代码基底，**以 DevTeam 为主导理念**进行改造融合。

### 前台可行性判断（依据实勘）

✅ **可行**，理由：角色与流水线均为 JSON 配置驱动，改造主体是替换配置而非重写引擎。

两边角色已大面积重叠：

| DevTeam 角色 | MyAgent 现有对应 |
|---|---|
| 前台接待 | `master`（调度 + 防火墙） |
| 开发教练 | `coach` |
| 界面设计员 | `designer` |
| 开发员 | `developer` |
| 架构巡检员 | `inspector` |
| 测试员 | `tester` |
| 部署运维员 | `deployer` |
| 信息侦察员 | `knowledge_retriever` |

**DevTeam 独有、需新增的高价值角色**：项目接手员、方案评审员、人事调配员、API 契约设计师、前端检修员、经验评估员、监控秘书。

### DevTeam 理念对应的评分得分点（改造价值所在）

| DevTeam 设计 | 对应评分项 |
|---|---|
| 17 人四层结构 + 完整 SOP | 任务定位与创意场景（20 分） |
| Phase 0→1→1.5→2→3 阶段调度 | 多步骤任务规划 |
| 三层记忆（上下文/经验/知识） | 本地多轮记忆 + RAG |
| 经验效用评分（Harvard 论文依据） | 核心能力完整性（20 分）+ 创意分 |
| 信息防火墙 + 上下隔离 | 权限控制与隐私保护机制 |
| 知识库保质期机制 | RAG 完整性 |

参考文档：`G:\workburddy\workspace\dev-team-platform\ARCHITECTURE.md`（含 17 个角色完整 prompt，可直接取用）

---

## 六、交接说明

**路由目标**：项目接手员（存量改造 + 从未跑通 → 需先诊断再动刀）

### 建议优先级

1. **P0 — 先跑通**：本地把 MyAgent 启动起来（可先用 CPU 或任意可用推理后端顶替），确认 13,271 行代码不是空壳
2. **P0 — 减重**：按用户诉求裁掉过重模块，聚焦评分表要的能力
3. **P1 — 换编制**：`role_pool.json` + `workgroups/*.json` 替换为 DevTeam 编制与 SOP，角色 prompt 从 ARCHITECTURE.md 搬运
4. **P1 — 清合规**：移除云端 API 降级通道，修正部署脚本显存参数
5. **P2 — 上机剧本**：排到实例前，把部署 → 验证 → 压测 → 录屏的完整步骤写成可照抄的清单
6. **P2 — 出口材料**：英文文档、架构图、PPT、PR — 由前台接待承接

### 止损提醒

时间仅 60 小时且用户为小白。**同一问题 2 次失败必须停下来重新评估路径**，不要在单点上耗尽时间窗口。宁可交一个功能收敛但完整可演示的作品，也不要交一个功能齐全但跑不起来的作品 —— 演示视频必须拍出真实运行画面。

---

# 补充章节 · DevTeam 完整蓝图消化（2026-08-04 12:5x）

> 用户追加提供 `G:\workburddy\workspace\dev-team-platform\BLUEPRINT.md`（512 行，比 ARCHITECTURE.md 完整得多）。
> 本章节为前台接待消化后的**落地转译**，非原文复述。已对照 MyAgent 实际代码逐条核实。

## 七、蓝图核心：记忆系统是主角，不是配角

BLUEPRINT 的第一句话就是纠偏：

> **角色拆分不是目的，记忆系统才是让拆开的 17 个人真正像一个团队的根本。**
> 没有记忆的 17 个 Agent = 17 个互不相识的陌生人。

**这句话改变了改造的重心。** 之前的方案把重点放在"换角色编制"，蓝图明确说那只是表象。四层记忆结构才是核心：

| 层级 | 负责角色 | 管什么 | 关键机制 |
|---|---|---|---|
| 团队·上下文记忆 | 监控秘书 | "做到哪了" | **覆盖不追加**（永远只留最新 1 条 ≤200 字）+ 用完即弃 |
| 团队·经验记忆 | 经验评估员 | "上次怎么踩坑的" | **效用评分淘汰**（+1/-1/-2，池满 15 淘汰最低分，≤-3 立删） |
| 团队·知识记忆 | 信息侦察员 | "现在该知道什么" | **保质期管理**（技术 6 月/安全 3 月/平台 2 月，过期归档不删） |
| 个人·独立作用域 | 教练 spawn 时注入 | "我被打回过几次、为什么" | **信息防火墙**，每人只在自己边界内积累 |

**设计依据（写进参赛文档能加创意分）**：
- 经验效用评分 ← Harvard《How Memory Management Impacts LLM Agents》(Xiong et al., 2025)，核心结论：**add-all 比一条都不存还差**，坏经验会自我放大传播
- 知识保质期 ← MIT 研究：技术文档半衰期 18 个月，**73% 的 RAG 系统 90 天内准确率显著下降**

---

## 八、🔴 重大发现：Prompt 全线超标 —— 这是 20 分速度分的金矿

### 8.1 事实

BLUEPRINT 给出的提示词长度纪律：

| 角色类型 | 目标字数 |
|---|---|
| 简单工具人 | 300-500 字 |
| 标准执行者 | 600-900 字 |
| 复杂决策者 | 900-1200 字 |

> 超过 2000 词元开始退化，**幻觉率 +34%**；超 2500 词元后开头规则被近因偏差削弱。

**MyAgent 实测（18 份角色 prompt，全部超标 2.5–8 倍）**：

```
7201 字  coach              ← 超标 6-8 倍
5371 字  secretary
5303 字  designer
4970 字  visual_analyzer
4815 字  tester
4457 字  cleaner
4321 字  developer
4209 字  master
4157 字  knowledge_retriever
4108 字  deployer
4084 字  inspector
3961 字  creative
3879 字  handoff_receiver
3832 字  scheduler
3547 字  hr_manager
2821 字  quality_checker
2565 字  writer
2412 字  translator
────────────────────────────
合计 ≈ 73,600 字（约 5-7 万词元）
```

### 8.2 为什么这是钱

**这不只是质量问题，这是 GPU 推理速度问题。**

每次角色被调用，这几千字 prompt 都要走一遍 prefill。在本地 Radeon GPU 上，prefill 阶段是实打实的算力消耗和首 token 延迟来源。

```
现状：coach 单次调用 prefill ≈ 5000+ 词元
精简后：≈ 700 词元
→ prefill 词元量下降约 85%
→ 首 token 延迟（TTFT）显著下降
→ 多角色流水线（dev_full 跑 7 角色）收益 ×7
```

**评分表第 4 项「针对推理速度的定向优化」= 20 分。** 这一项需要的正是"我做了 X 优化，实测从 A 提升到 B"的硬数据。

Prompt 精简的独特价值：**它是唯一一个既符合 DevTeam 蓝图纪律、又能直接产出速度对比曲线的改造项**。一份改造同时吃两块分。

### 8.3 落地建议

- 按蓝图纪律把 18 份 prompt 压到 600-900 字（简单角色 300-500）
- 详细骨架**不进 prompt**，改为 `templates/` 文件 + prompt 内写引用（"读 templates/xxx.md"），角色运行时按需 Read
- 负向约束每角色最多 3-5 条，**每条必须带正面替代方案**（蓝图的"粉红大象"原则：`not` 是 1 个词元，被否定的概念可能占 5 个，否定信号打不过概念激活）
- 🎯 **上云时必须做 A/B 实测**：精简前 vs 精简后的 TTFT、tokens/s、显存占用 —— 这组数据直接进参赛文档的「推理速度优化说明」

---

## 九、角色映射修正 —— 比原判断乐观得多

**⚠️ 更正第五章的映射表。** 实读 `role_pool.json` 后确认，MyAgent 已有 **17 个角色**，DevTeam 缺口只有 3 个，不是原先判断的 7 个。

### 9.1 已有且直接对应（14 个）

| DevTeam 角色 | MyAgent 现有 id | 备注 |
|---|---|---|
| 前台接待 | `master` | 调度 + 防火墙 |
| 开发教练 | `coach` | |
| **项目接手员** | `handoff_receiver` | ✅ 原判断说缺，实际有 |
| **监控秘书** | `secretary` | ✅ 原判断说缺，实际有 |
| **人事调配员** | `hr_manager` | ✅ 原判断说缺，实际有 |
| **方案评审员** | `quality_checker` | ✅ 原判断说缺，实际有（需改造语义） |
| 信息侦察员 | `knowledge_retriever` | |
| 界面设计员 | `designer` | |
| 开发员 | `developer` | |
| 架构巡检员 | `inspector` | |
| 测试员 | `tester` | |
| 部署运维员 | `deployer` | |

### 9.2 真正缺失（3 个，均为纯新增）

| 缺失角色 | 价值 | 建议 |
|---|---|---|
| **经验评估员** `experience-evaluator` | 🔴 高 — 蓝图记忆系统的支柱之一，Harvard 论文依据，创意分核心 | **必做** |
| API 契约设计师 `api-contract-designer` | 🟡 中 — 前后端并行开发的前提 | 可选 |
| 前端检修员 `frontend-debugger` | 🟡 中 — 建造/诊断分离原则的体现 | 可选 |

### 9.3 MyAgent 多余角色（对应用户"太重"的直觉）

`creative` / `translator` / `visual_analyzer` / `writer` / `scheduler` / `cleaner`

这 6 个不在 DevTeam 编制内，合计 prompt 约 22,000 字。**建议移出主流水线**（保留文件，不注册进 dev_full），既减重又减 prefill 负担。

---

## 十、记忆系统缺口核查（实读代码）

`backend/core/memory/` 共 **2,930 行**，家底不薄：

```
437 blackboard.py      ← 共享黑板（对应蓝图的跨角色协同）
424 knowledge_base.py  ← 知识库（三元组，非向量）
414 compressor.py      ← 上下文压缩（对应秘书的"覆盖不追加"雏形）
403 working_memory.py  ← L0 工作记忆
396 exporter.py
351 session_memory.py  ← L1-L2 会话记忆
293 archive.py         ← 归档（对应蓝图的过期归档）
171 store.py
```

**但蓝图最有分量的两个机制，全库为零**：

| 蓝图机制 | MyAgent 现状 | 工作量判断 |
|---|---|---|
| **经验效用评分**（+1/-1/-2、池满淘汰、≤-3 立删） | ❌ 仅 `agents.py`/`orchestrator.py` 有零星词汇，无实现 | 纯增量，不动现有引擎 |
| **知识保质期**（created/verified/expires/source/triggers 元数据） | ❌ 全库 0 处，仅 loader.py 注释里提了一句"忽略过期内容" | 纯增量，给 knowledge_base 加元数据字段 |

**结论**：这两块是**加法不是改法**，风险低、增分高。它们同时命中评分表的「核心能力完整性（20 分）」和「任务定位与创意（20 分）」。

---

## 十一、排期修正（基于蓝图，覆盖第六章建议）

现在时间 **2026-08-04 12:5x，剩余约 59 小时**。

| 优先级 | 任务 | 工时 | 对应评分 |
|---|---|---|---|
| **P0** | 关闭 `cloud_api_enabled` + 移除 ZHIPU + 清 chromadb 依赖 | 3h | 🔴 合规红线（资格问题） |
| **P0** | **18 份 prompt 按蓝图纪律精简至 600-900 字**，详细内容外移 `templates/` | 8h | 速度优化 20 分 + 质量 |
| **P0** | 裁掉 6 个非编制角色，dev_full 收敛为 DevTeam 编制 | 2h | 减重（用户诉求） |
| **P1** | 新增**经验评估员** + 效用评分机制（+1/-1/-2、15 条淘汰） | 6h | 核心能力 20 分 + 创意 20 分 |
| **P1** | 知识库加保质期元数据（created/verified/expires/triggers） | 4h | RAG 完整性 |
| **P1** | 秘书行为对齐蓝图：摘要覆盖不追加 ≤200 字、文档用完即弃 | 3h | 多轮交互流畅度 20 分 |
| **P1** | 修正 `setup_amd_cloud.sh` 显存参数（现按 192GB MI300X 写） | 4h | 上云前提 |
| **P2** | 上云一次成型：部署 → 验证 → **prompt 精简 A/B 测速** → 采数据 | 8h | 🎯 速度优化 20 分的实证 |
| **P2** | 录制演示视频（真实 Radeon GPU 画面） | 6h | 必交项 |
| **P3** | 信息防火墙落地（spawn 时注入"上次产出摘要"） | 4h | 权限隐私 + 创意 |
| — | 出口材料（英文文档/架构图/PPT/PR） | 6h | 由前台承接 |
| — | 缓冲 | 5h | |

### 明确砍掉

- 向量 RAG（`chromadb` 全库未 import，现有三元组够用）
- `domain/` 死代码孤儿
- 智谱云降级通道（合规）
- 6 个非编制角色（保留文件不注册）
- API 契约设计师、前端检修员（时间不够则跳过，非核心分点）

### 用户偏好红线（务必遵守）

> **任何 UI / 架构层面的实质改动，必须先出方案给用户确认，不许直接动源码。**

适用范围：prompt 精简方案、角色编制调整、记忆系统新增设计 —— **这三项都必须先出方案再动手**。
不适用：依赖清理、配置开关、显存参数修正等无争议的合规/修复类改动。
